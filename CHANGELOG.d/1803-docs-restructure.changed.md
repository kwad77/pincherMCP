Documentation restructure (#1803): the README is rewritten as a lean landing page (424 → ~160 lines) and the 1147-line `docs/REFERENCE.md` is split into a focused `docs/reference/` folder (`architecture.md`, `tools.md`, `pinchql.md`, `languages.md`, `http-api.md`, `cli.md` + an index). The six REFERENCE.md parity-test gates and all repo-wide links are re-pointed at the new files; no behavior change.
