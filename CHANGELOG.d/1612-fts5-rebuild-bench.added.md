`BenchmarkRebuildFTS_GoProject` / `_K8sOps` / `_NodeMonorepo` — committed bench coverage for the FTS5 escape-hatch path (`Store.RebuildFTS()`). The path drops every per-corpus FTS5 vtab + sync triggers, recreates from canonical DDL, and bulk-loads from `symbols` — seconds-to-minutes on large repos. Pre-fix we had zero defendable perf floor on this surface; now we do. Doctor advisory on FTS5 fragmentation + the chunked-rebuild stretch goal (the other halves of #1612) follow in a sibling PR. (#1612)
