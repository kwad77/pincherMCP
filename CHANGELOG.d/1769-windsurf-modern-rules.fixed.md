`pincher init --target=windsurf` now writes the modern Windsurf rules-directory path (#1769 v0.91). The target wrote `./.windsurfrules` — a single project-root file. Modern Windsurf (Wave 8+) reads `.windsurf/rules/*.md`: one markdown file per rule, each with a `trigger:` activation-mode frontmatter. The legacy single file still works for backward compatibility, so this is a modernization rather than a fix — but it brings the target in line with the documented convention, mirroring the `cursor` target's `.cursor/rules/pincher.mdc` shape. The target now writes `./.windsurf/rules/pincher.md` with `trigger: always_on` frontmatter (the pincher tool-usage policy belongs in every Cascade system prompt); `DetectFn` keys on the `.windsurf/` directory; re-runs touch only the marker block so the frontmatter survives policy refreshes. Completes the host-integration path sweep alongside #1765 (Antigravity) and #1767 (JetBrains Junie). Docs (`docs/index.html`, `docs/REFERENCE.md`) updated in lockstep.
