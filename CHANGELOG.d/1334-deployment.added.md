**docs:** Deployment guides directory at `docs/deployment/` ([#1334](https://github.com/kwad77/pincher/issues/1334) — Phase 3 docs completeness dogfood-3). Adds `docker.md` (single-container quickstart + docker-compose + indexing host directories + resource sizing on the pincher-repo corpus), `systemd.md` (Linux user service install + drop-in override + journalctl recipes + linger configuration), `homebrew.md` (macOS+Linux tap install + `brew services` setup + tap-maintainer manual-bump notes), and `README.md` deployment hub with decision matrix + common ops concerns (HTTP auth, state directory, upgrades, observability). Replaces the previous "only helm.md" state where install-path quickstart was scattered across `packaging/README.md` without a docs-tree entry.
