**REFERENCE.md tool-listing parity ([#672](https://github.com/kwad77/pincher/issues/672) workstream 4 v0.79 hardening):** new `TestReferenceMD_EveryRegisteredToolMentioned` asserts every tool registered in `registerTools()` has a backticked mention in `docs/REFERENCE.md`. Caught: `context_for_task` (#1259 v0.71 composite-investigation tool) was registered and counted in the \"23 MCP tools\" metadata line but had NO row in any tool table — a user reading REFERENCE.md and counting rows would only find 22. Inline fix adds a row to the Search & graph table; the parity test prevents future tools from shipping uncounted. Complementary to `TestReferenceMD_ToolCountParity` (#1506) — that one pins the count, this one pins each name.
