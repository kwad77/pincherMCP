Added a watcher-faithful incremental-resolve regression harness (`internal/index`, [#1772](https://github.com/kwad77/pincher/issues/1772)) — it reproduces a real watcher tick (`changedFiles` → `invalidateReferencers` → `Index`) and snapshots every CALLS edge with its `source` column, so any future incremental-resolve graph degradation fails a test with a per-edge, `per_file`-vs-`resolve_pass`-tagged diff.
