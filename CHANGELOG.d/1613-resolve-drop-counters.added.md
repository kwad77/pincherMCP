Per-reason drop counters for `resolveImports` / `resolveCalls` / `resolveReads` — extends #1613 v0.85's wrapper-level observability (`pincher.resolve.summary` with aggregate `dropped`) by emitting per-resolver `pincher.{imports,calls,reads}.resolve.summary` slog.Info lines carrying split counters when drops > 0. Mirrors the existing `pincher.uses_var.resolve.summary` shape that #1500 added for #1479. Splits the aggregate dropped count into actionable signal: `dropped_from_missing` (extractor produced unbindable FromQN), `dropped_to_missing` (no in-project target after every fallback), `dropped_self_edge` (fromID == toID), `dedupe_count` — plus IMPORTS's `external_synthesized`, CALLS's, and READS's richer taxonomy (`dropped_lang_mismatch`, `dropped_polymorphic_method`, `dropped_struct_field_read`, `dropped_not_variable`). Quiet on no-drops happy path. (#1613 follow-up)
