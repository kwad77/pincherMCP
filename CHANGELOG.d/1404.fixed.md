`pincher doctor --project NAME` / `pincher verify --project NAME` and the MCP `doctor` tool's `project` arg now use tiered match (exact id → exact case-insensitive name → substring fallback), matching `pincher project rm`'s shape. Pre-fix the flat substring check over-matched on nested project IDs — e.g. `--project pincher-repo` returned every corpus sub-project under `pincher-repo/testdata/` whose id contained the parent path as a substring. Same dogfood loop that filed #1399 / #1401 caught this immediately after both PRs shipped. (#1404)
