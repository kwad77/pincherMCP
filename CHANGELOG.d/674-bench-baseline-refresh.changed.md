Refreshed the committed performance baseline (`testdata/bench/{index,server}.bench.txt`) for v0.89 (#674 workstream 2). The GitHub Actions runner pool rolled from `AMD EPYC 7763` to `AMD EPYC 9V74`, so the previous baseline — pinned to the old SKU — made every `make corpus-bench` comparison conflate the CPU change with real deltas. The v0.89 perf-regression validation against the stale baseline showed zero wall-clock regressions (index 0.8–7% faster, server 3.7–20% faster — the newer SKU plus no code regression); allocs/op held flat except a 2–5% rise on the cold-index benchmarks, well inside the +30% bench gate. New benchmarks added since the last refresh (`RebuildFTS`, `HandleArchitecture`, `Auth_TimingProfile`) are now captured in the baseline. Per the CLAUDE.md release checklist, `.x9` hardening releases refresh the baseline; the trigger is `.github/workflows/bench-baseline.yml`.
