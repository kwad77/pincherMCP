`search`: when the project's `binary_version` is older than the running server (the index_drift direction surfaced in `health`) and no relaxation rescues the query, stamp `_meta.empty_reason=stale_index` instead of `no_results_in_corpus`. CALLS/resolution edges may reflect older extraction rules — the empty result might be a false negative the user can fix by re-indexing. `next_steps` lead with `index force=true` then `search` retry. Closes the last tractable orphan from #1603's audit (12 stamped / 1 orphan — `same_file_only` remains, deferred because it requires same-file caller detection inside trace).
