`changes` accepts git revspec operators in `scope=base:<rev>` (`HEAD~5`, `HEAD^`, `master~3`) instead of rejecting them with "disallowed character" — the downstream `git rev-parse --verify` is the real gate, and `exec.Command` bypasses the shell so revspec chars are passed literally (#1433).
