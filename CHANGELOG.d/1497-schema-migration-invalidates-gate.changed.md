**Schema-migration `invalidatesNothing` gate suppresses force-reindex cascade ([#1497](https://github.com/kwad77/pincher/issues/1497) v0.83):** the `binaryDriftForce` branch in `Index()` (which re-extracts every file when the indexer detects a binary-version mismatch) now consults `Store.LastStartupMigrationInvalidates()` and suppresses the force-reindex when the migrations that ran during this process's `Open()` are all `invalidatesNothing`-classified (DDL-only entries that don't touch extractor surface). Pre-gate, every `make install` + `/mcp` reconnect triggered full re-extraction of every project regardless of whether the binary's extractor side actually changed — for the pincher-repo storm scenario (~700 files × 30-40ms × 6 parallel projects = ~150s cumulative), the gate cuts the cost to a version-stamp update and a no-op walker pass. The gate keeps force-reindex when no migrations ran this startup (binary changed for non-schema reasons — conservative; the schema-migration classification can't tell us why), when union is `invalidatesAll` (the 5 currently-classified All migrations — v18→v19, v19→v20, v22→v23, v24→v25, v32→v33), and when union is language-scoped (selective per-language `file_hash` clearing rolls forward to #1543 v0.84). New `internal/index/binary_drift_gate.go` exposes the pure-function gate; truth-table test covers the four corners of `(migrations-ran × All × Languages)` so the safety case stays pinned against future "simplifications" of the gate.
