**why_empty: deterministic known-reasons list in error response (#1580).** When the caller passed an unknown `prior_empty_reason`, the error message listed every known value via `for k := range whyEmptyCatalog` — and Go map iteration randomises order per call, producing different byte-strings per invocation. Regression test pins the deterministic output across 5 successive invocations. Five-line fix (sort.Strings before join). Part of the composite-hardening umbrella #1582.
