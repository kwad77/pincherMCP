**Slog-everywhere convention pinned ([#672](https://github.com/kwad77/pincher/issues/672) workstream 4 v0.79 hardening):** CLAUDE.md `§ Idioms` says \"`slog` everywhere. `log.Printf` will silence under bench `TestMain` and corrupt baselines.\" One site violated: `cmd/pinch/main.go:286` used `log.Printf("pincherMCP: http server error: %v", err)` for the goroutine that wraps `srv.ListenAndServeHTTP`. Converted to `slog.Error("pincher.http_server.error", "err", err)` matching the existing `slog.Error("pincher.schema_drift.detected", ...)` shape in `internal/server/server.go`. New `TestProductionCode_NoLogPrintfPattern` walks `internal/` + `cmd/` (non-test) and asserts zero `log.{Printf,Print,Println}` calls. `log.Fatalf` for startup-error exits stays legal (no slog Fatal equivalent); `log.SetOutput` / `log.SetFlags` for pre-main configuration stays legal.
