`health` response's `project` field now surfaces `id`, `schema_version_at_index`, and `last_indexed_branch` (when non-empty) — bringing it into alignment with `architecture`'s `project` field for the shared structural fields. Pre-fix the hand-rolled map literal silently dropped #1388's `last_indexed_branch` rename and never carried `id` / `schema_version_at_index` at all, forcing integrators to call `list` separately to get the project's stable handle. (#1410)
