**New `target_not_resolved` empty-reason code (#1578).** Adds `EmptyReasonTargetNotResolved` to the FILE-O catalog for the distinct "your input shape passed the resolution heuristic but didn't match any indexed symbol" case. Previously composites stamped `EmptyReasonNoResultsInCorpus` here — which signals "the data isn't there" and pushes agents toward the wrong recovery (`doctor`, re-index, widen filters). Now `plan_change` and `investigate_failure`'s parse-empty branch correctly stamp `target_not_resolved`, paired with recovery steps that suggest `search` to confirm what BM25 surfaces + `list` to verify project scope. `docs/empty-reasons.md` extended to 13 codes; `whyEmptyCatalog` adds the matching entry; `TestWhyEmpty_CatalogCoversEveryConstant` + `TestWhyEmpty_NoUnknownCatalogEntries` updated. Two existing tests updated to assert against the new code (their previous assertion encoded the bug). Part of composite-hardening umbrella #1582.
