GitHub Pages docs (`docs/index.html` + `docs/release-channels.md` + `docs/streamable-http.md`) caught up to v0.72 and re-framed for vocabulary discipline. The OG `<meta description>` and a "Routing-enablement" card on the landing page framed `_meta` as "the routing layer" — that's the wrong frame (pincher is the foundation; `_meta` is the enabler; the LLM in the host's loop is the router). Re-worded to match the integration-docs vocabulary (`docs/integrations/loop-leverage-layers.md` + `meta-envelope-contract.md`). Two stale roadmap badges flipped: "Cross-file resolution v0.67 → v0.69" rewrote as a today-state snapshot naming TS receiver-type (#1177 v0.72) and Swift regex promotion (#1450 v0.73); "Observability standardization v0.67" flipped to past-tense since OTLP + Prometheus + X-Request-ID all shipped. Schema-version tags bumped v32 → v33 (caught up to the v0.72 #1421 schema migration). v0.72 release-prep PR (#1448) missed these; backfilled here per the CLAUDE.md release-prep checklist's docs/ audit grep.
