**Resource-pressure bench scaffolding (FILE-I, #1528).** Synthetic-corpus generator (`scripts/generate-synthetic-corpus.sh`) + peak-RSS bench (`scripts/resource-pressure-bench.sh`) + weekly advisory CI gate (`.github/workflows/resource-pressure.yml`). Bench runs `pincher index` under `/usr/bin/time -v` against four tiers (1k / 10k / 50k / 100k) and records peak RSS; CI runs the 1k + 10k tiers on cron (Tuesday 13:00 UTC), 50k/100k via `workflow_dispatch`. Output anchors the "minimum recommended RAM per indexed-file tier" budget in `docs/REFERENCE.md`. **Phase 1 (v0.85-v0.89): advisory**; **Phase 2 (v0.90+): hard budget** per FILE-I acceptance — flip is two lines. Methodology + phase rollout in [docs/methodology/resource-pressure.md](docs/methodology/resource-pressure.md). Linux-first (Windows requires pagefile/commit-charge instrumentation, deferred to v1.x). FILE-I acceptance bullets #3 (graceful degradation) and #4 (OOM clean-abort test) deferred to v0.86/v0.87 follow-ups since they need runtime indexer changes; this scaffolding lets the budget PUBLISH at v0.85 with real numbers before those harder lifts.
