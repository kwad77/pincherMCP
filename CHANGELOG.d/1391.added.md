**docs:** Phase 4 composite-tool roadmap at `docs/integrations/composite-tool-roadmap.md` ([#1391](https://github.com/kwad77/pincher/issues/1391)). Names the five candidate composites slated for v0.81–v0.90 — `investigate_failure`, `audit_unused`, `plan_change`, `onboard_module`, `why_empty` — with today's atomic-call sequence each replaces, the synthesized output shape per composite, and the contract invariants (additive only, no internal MCP round-trips, single envelope, per-composite `_meta.diagnosis_v2` codes, mandatory `empty_reason` on zero-result, idempotency) every composite must hold. Pairs with `loop-leverage-layers.md` (Layer 3 — composite call) and `meta-envelope-contract.md` (envelope shape).
