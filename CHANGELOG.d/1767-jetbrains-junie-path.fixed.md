`pincher init --target=jetbrains` now writes the path JetBrains Junie actually reads (#1767 v0.89). The target (#1335) wrote `./.idea/.junie/guidelines.md` — nesting `.junie/` inside the IDE-config directory `.idea/`. Per JetBrains' Junie documentation, Junie reads guidelines from the `.junie/` directory at the project ROOT (a sibling of `.idea/`): `.junie/AGENTS.md` (modern, preferred), `.junie/guidelines.md` (legacy, still supported), then root `AGENTS.md`. The nested `.idea/.junie/` file was never read. The target now writes `./.junie/guidelines.md` — the legacy-but-supported, Junie-specific path, the right pincher-owned file (writing `AGENTS.md` would clobber the user's shared cross-tool rules). `DetectFn` keeps keying on `.idea/` — still the correct "is this a JetBrains IDE project" marker. Same drift class as the Antigravity target fix (#1765); both surfaced in a sweep of pincher's host-integration paths. Docs (README, index.html, jetbrains tutorial + benefits page, loop-leverage table) and the tool-contract snapshot updated in lockstep.
