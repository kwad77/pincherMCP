**docs:** README leading paragraph audited for v0.75 framing discipline ([#1334](https://github.com/kwad77/pincher/issues/1334) Phase 3 docs completeness). Two corrections: (1) "The retention is routing" reframed as "The retention is the planning-loop surface" — pincher is the foundation, `_meta` is the enabler, the LLM in your host's loop is the router (matches `docs/integrations/loop-leverage-layers.md` vocabulary). The expanded `_meta` field list now names `next_steps`, `empty_reason`, `warnings_v2` alongside the legacy `capabilities` / `complexity_tier` / `X-Request-ID` triad. (2) "Sourcegraph, OpenGrok, and IntelliJ index a codebase for humans browsing it" reworded to "Conventional code-search tools..." — name-dropping competitors invites comparison scrutiny pre-1.0 (per the no-comparison-docs-pre-1.0 discipline). The SHAPE distinction stays intact; the competitor names just drop. Forward-link to `loop-leverage-layers.md` added in-paragraph so readers landing on the README leading copy can immediately follow the framing thread.
