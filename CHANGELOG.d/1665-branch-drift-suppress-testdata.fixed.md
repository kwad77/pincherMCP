Branch-drift advisory now suppresses testdata corpora + worktree-staging dirs (#1665 v0.87). Same dogfood pattern as #1644's nested-project suppression — projects under `testdata/corpus/`, `testdata/__fixtures__/`, or `.atrium/work/` are static testdata fixtures, not real working trees that experience meaningful branch drift. Pre-fix the advisory fired on 8 testdata corpora every time the parent repo switched branches, claiming "symbol byte-offsets may point at the wrong spans" — false. The remediation (`pincher index <path>`) just churned the index without fixing anything because the testdata content doesn't move with the parent's branch. New `branchDriftSuppressedPath` helper mirrored to both MCP doctor (`internal/server/admin.go`) and CLI doctor (`cmd/pinch/doctor.go`) per the bounded-duplication convention. Matrix test covers all three suppression segments plus substring-overlap controls (`testdata-for-other-thing`, `corpus_data`).
