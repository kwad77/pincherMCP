**govulncheck CI gate + dependency upgrade procedure ([#1525 FILE-F](https://github.com/kwad77/pincher/issues/1525) v0.88):** new `.github/workflows/govulncheck.yml` runs `golang.org/x/vuln/cmd/govulncheck@latest` on every push + every PR + weekly on Monday at 09:00 UTC. Advisory phase per acceptance criteria — failure doesn't block merge until one full release window of green runs proves the gate is stable; then flips to required via branch protection. The `-test -show traces` flags scan test code too and print affected call paths so the failure is actionable (vs the bare CVE ID requiring another lookup). New `docs/security/dep-upgrade-procedure.md` documents the operational mechanics: routine non-MCP-SDK dep upgrades (identify via traces, upgrade via `go get -u`, verify with `go test ./...` + `make corpus-test`); the special-case MCP SDK upgrade path (read changelog → test against every supported host via FILE-M canonical workflows → bump in a PR by itself → soak through one `.x9` hardening release before promoting to stable); exception management when no fixed version exists upstream (SECURITY.md registry forthcoming with FILE-D). Pairs with FILE-E (#1524 artifact signing) and FILE-D (#1523 security audit pass) as the v0.87-v0.88 supply-chain hardening triplet.
