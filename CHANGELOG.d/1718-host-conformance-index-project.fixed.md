host-conformance replay now indexes a project so the canonical workflow's `search` and `architecture` calls resolve (#1718 v0.89). With the stdin-EOF crash fixed (#1707) the replay reached request id=3 — a `search` tool call — and failed on every host with "line id=3 returned an error envelope": `search` against an empty server returns "no project specified and no session project detected", and the harness indexed nothing (its comment wrongly claimed the flow needs no project). A real MCP host scopes such calls via MCP roots advertised at initialize; the replay corpus carries no roots. `scripts/host-conformance.sh` now indexes the pincher repo once (the corpus's `search "Indexer"` deliberately targets pincher's own `index.Indexer` type), shared across all six hosts, and injects the resulting project name into every replayed `tools/call` payload — the corpus files stay canonical wire-shape on disk; `project` is a first-class argument every code-navigation tool already accepts. All six hosts (claude-code / codex / cursor / jetbrains / vscode-copilot / zed) now replay the full 4-request workflow green. Companion hardening: the required-tools assertion strips `\r` from jq output so the gate also runs under Git Bash, where a Windows jq.exe emits text-mode `\r\n` (no-op on the Linux CI runner).
