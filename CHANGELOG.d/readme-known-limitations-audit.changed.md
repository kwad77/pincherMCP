**README + REFERENCE Known limitations audit:** removed two stale claims and added one current one. **(1)** README item "Edge graph: Go + Python with full IMPORTS/CALLS resolution; every other extracted language with same-file CALLS only" → updated to reflect [#1177](https://github.com/kwad77/pincher/issues/1177) closing in v0.72: TS/JS CALLS with non-empty `ReceiverType` now defer for cross-file resolution. The "long tail" pointer narrowed from "TypeScript receiver-type binding, Rust/Java AST, etc." to just Rust ([#1182](https://github.com/kwad77/pincher/issues/1182)) + Java ([#1183](https://github.com/kwad77/pincher/issues/1183)), with v0.85 milestone naming. **(2)** REFERENCE item "~7 non-Go languages still use regex extraction" → fixed to the actual count: 16 regex-tier extractors (9 at the 0.85 stable tier, 7 at 0.70 approximate tier — per v0.73 / v0.63 promotions), 10 AST extractors at 1.0, 1 stub (Haskell). The pre-fix "~7" was a v0.6-era estimate that drifted through the regex-tier promotions without anyone catching it. **(3)** Added a new current limitation: "Binary-version bump triggers full project re-extract" — schema-migration invalidates every project's last-indexed schema stamp so every file is re-extracted regardless of whether the migration actually changed any specific language's extractor. v0.78.1 reduces the per-pass cost; the per-migration "affected extractor set" filter that would let unchanged languages skip the cascade entirely is tracked under [#1497](https://github.com/kwad77/pincher/issues/1497).
