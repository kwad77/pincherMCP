Per-language extraction breakdown — `pincher.index.extraction.by_language` slog.Info line emitted at end of extraction when totalFiles > 0, carrying `lang_N_name` / `lang_N_ms` / `lang_N_files` for the top 10 languages by duration plus `other_ms` / `other_files` / `other_languages` aggregating the tail. Closes the gap left by the aggregate extraction timing in #1620: first dogfood showed extraction is 82% of force-reindex cost, but we couldn't see WHICH extractor dominated. Now we can. Sorted by duration desc so the dominant cost reads first. (#1613 follow-up)
