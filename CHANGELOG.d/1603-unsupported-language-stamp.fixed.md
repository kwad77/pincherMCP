`search`: when a stub-tier `language` filter (currently only Haskell — `confidence=0`, no real extractor) yields zero results, stamp `_meta.empty_reason=unsupported_language` instead of letting the relaxation verifier rescue the query with `query_too_narrow` ("drop the language filter"). The verifier's stamp was technically correct but misleading: the language is structurally broken for symbol extraction, not a filter-ergonomics issue. Closes another orphan from #1603's audit (now 11 stamped / 2 orphan: `stale_index`, `same_file_only` remain).
