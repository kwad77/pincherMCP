Regression tests pin `HealthCheck` + `handleHealth` extraction_coverage to the queried project (#1476). Bug was reported against binary v0.73 with the symptom "health(project=X) returns language counts from OTHER indexed projects." Verified on current master via two new tests (Store-layer + handler-layer) — the bug is no longer present (the SQL already had `WHERE project_id = ?`), but the contract was untested across projects. Pinning prevents regression as the multi-project surface evolves.
