**fix(cypher + server):** Hardcoded `knownEdgeKinds` allowlists now include `USES_VAR` ([#1480](https://github.com/kwad77/pincher/issues/1480)). `#1380` (`USES_VAR` Ansible Jinja-substitution dataflow edges, v0.71) shipped the extractor + resolver but missed updating the two `knownEdgeKinds` allowlists at `internal/cypher/engine.go:494` and `internal/server/server.go:7133`. Pre-fix, `MATCH (a)-[:USES_VAR]->(b)` returned 0 rows AND a misleading `"edge kind \"USES_VAR\" not recognized"` warning even when `USES_VAR` rows existed in the DB. Post-fix `USES_VAR` is a recognized kind in both query and trace allowlists; the unknown-kind warning text now enumerates `USES_VAR` in its "valid kinds" list so agents who typo it discover it exists. Pinned by `TestExecute_USES_VAR_EdgeKindRecognized` (positive) + `TestExecute_UnknownKindWarning_NamesUSES_VAR` (cross-check). Structural fix (single source of truth for edge kinds — registry the indexer + cypher + server all import) tracked separately; this PR is the short-term two-line patch the issue suggests.
