`trace` now suggests the reverse direction in `_meta.next_steps` when an inbound or outbound trace returns 0 hops (#1634 v0.85). Pre-fix the empty-trace branch only suggested reading the symbol's own source — a confidently-wrong move on hub functions where the other-direction edge set was populated. Builds on #1632's audit-vs-unexpected zero-result split to drive the retry-recovery rate up.
