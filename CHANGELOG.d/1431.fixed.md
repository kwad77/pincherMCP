`trace` now scopes-to-session-first when no `project=` arg is passed — same fix the symbol + context handlers got in #1409 and the neighborhood handler got in #1425. Pre-fix repro on the dogfood DB (pincher-repo + sniffer fork both have `internal/db/db.go::db.Open#Function`): `trace id=... direction=inbound depth=1` looked up via unscoped GetSymbol, hit sniffer's row first, emitted a misleading `"hops will silently be 0"` warning, then ran the BFS in pincher-repo's graph and returned 16 actual hops. The warning contradicted the data — agents had to guess which to trust. Now the seed scopes to the session project first; only falls back to unscoped GetSymbol when the seed genuinely isn't in session. The existing #1052 cross-project warning still fires correctly for genuine cross-project seeds (regression-tested). New `trace_scope_to_session_test.go` covers both the positive case (shared-ID prefers session, no false warning) and the negative case (only-in-other still warns). Final piece of the scope-to-session-first sweep across the retrieval-shape tools (symbol/context #1409, neighborhood #1425, trace #1431). (#1431)
