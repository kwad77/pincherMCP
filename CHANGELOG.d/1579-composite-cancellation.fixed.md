**Composite cancellation contract pin (#1579).** All four Phase 4 composites with `_ = ctx` discards (`investigate_failure`, `plan_change`, `audit_unused`, `onboard_module`, `why_empty`) plus the atomic `dead_code` handler now honor cancellation: entry-point `ctx.Err()` check at the top of each handler returns `errResultRich` immediately on pre-cancelled context, and the iteration-heavy composites (`investigate_failure`, `plan_change`, `audit_unused`) add per-iteration `ctx.Err()` breaks before each `TraceViaCTEScoped` call. FILE-H (#1527) cancellation test suite extended with 6 new `TestCancellation_HandleX_ReturnsPromptly` rows; the `TestCancellation_HeavyToolsHaveContractTest` gate's previous composite-exemption (which assumed cancellation was inherited via building-block calls) is removed — the gate now fails the build until every heavy tool has an explicit contract test. Part of composite-hardening umbrella #1582.
