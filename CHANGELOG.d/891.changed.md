`docs/REFERENCE.md` leading metadata line now reads `**Schema version:** v26` (was v25). The v25→v26 migration landed when #862 added `pending_edges.base_type` for type-info resolution; the reference doc didn't get the inline bump, which the release-prep checklist (CLAUDE.md item 5, post-#688) says must happen every release that moves any of the three leading numbers. Catch was triggered by a mid-cycle sanity scan rather than waiting for tag time — the README/REFERENCE drift is what users hit first when clicking through from the landing page.
