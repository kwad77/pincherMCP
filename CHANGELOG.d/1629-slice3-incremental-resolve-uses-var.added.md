Incremental resolve completed for USES_VAR (#1629 v0.87, slice 3). Closes out the #1629 incremental-tick perf umbrella: slice 1 (PR #1673) shipped IMPORTS, slice 2 (PR #1674) shipped CALLS + READS, this slice extends the same scoping pattern to the fourth resolver (Ansible USES_VAR, #1165). `resolveUsesVar` now accepts a `scopeFiles []string` parameter and uses the existing `DeleteResolvePassEdgesByKindForSourceFiles` helper from slice 1. The empty-scope edge case (a file that previously emitted USES_VAR edges and now emits none) mirrors the other three resolvers — direct resolver invocation from `Index()` to fire the scoped-delete-on-empty branch. With all four resolvers scoped, the full resolve block on a 1-file-change watcher tick now does O(changed) work instead of O(project) — the structural fix #1629 was filed for.
