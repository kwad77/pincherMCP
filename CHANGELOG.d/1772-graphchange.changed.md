Extended the #1772 incremental-resolve regression harness ([#1772](https://github.com/kwad77/pincher/issues/1772)) with a graph-changing layer (real call add/remove across a multi-hop `top→mid→leaf` referrer chain) and an env-gated real-corpus probe that indexes pincher's own `internal/` tree and runs eight incremental watcher ticks. Both keep the CALLS graph byte-identical — the incremental-resolve path is sound on real code; the reported session degradation is not a scope-deletion bug.
