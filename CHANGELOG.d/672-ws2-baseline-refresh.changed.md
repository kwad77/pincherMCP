**Bench baseline refreshed against v0.79 master ([#672](https://github.com/kwad77/pincher/issues/672) workstream 2 v0.79 hardening):** `testdata/bench/{index,server}.bench.txt` regenerated via `.github/workflows/bench-baseline.yml` (run [26054340216](https://github.com/kwad77/pincher/actions/runs/26054340216)) — Linux AMD EPYC 7763, `-4` GOMAXPROCS, `-benchtime=10s`. The old baseline was captured at PR #173 era (pre-v0.71, module path still `github.com/pincherMCP/pincher` before the rename to `kwad77`) and had drifted across 8 feature minors. The new baseline reflects real v0.79 perf shape: Cold index per-file got more expensive as we added languages (Scala/Lua/Zig/Elixir/Dart/R/HTML/XML extractors, branch stamping, USES_VAR resolution, structured `_meta.warnings_v2`); Incremental NoChange and every Server handler got dramatically faster (#1315 watcher hot-path -85% allocs, #1322 ApproxTokens cheap default cut 60% of authenticated-handler allocs). `make corpus-bench` now gates against this refreshed ground truth — the \"no perf regression >5%\" criterion in #672 §2 is enforceable for the first time since #173.
