`pincher doctor --fix` safe-action allowlist gains `prune-stale-failures` — deletes every extraction_failures row whose `last_seen_at` predates its project's `indexed_at` (the rows #1382's `is_stale` tag surfaces in doctor output). Live repro on this machine: the DB had 6058 truncated stale failures from workfixd TSX collisions recorded BEFORE #1377's QN-scope fix; workfixd hadn't been re-indexed since, so PruneExtractionFailuresForFile (#1319) couldn't clear them. The new action lets the operator clean them up without re-indexing every project. Safe per the allowlist posture: only ever removes rows the indexer would re-record on the next pass anyway if the underlying failure still reproduced. Implemented as a single DELETE ... WHERE EXISTS so the DB enforces the JOIN; new `Store.PruneStaleExtractionFailures()` writer method. Closes #1386.
