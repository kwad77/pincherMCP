Three Phase 4 composite tools hardened against silent-confidently-wrong output (v0.89, #1787/#1788/#1789). **`investigate_failure`** — the stack-frame tokenizer emitted Go import-path components (`github.com`, the org and repo names, `internal`) and panic-message English (`out`, `range`, `with`, `length`, `running`) as candidate symbol tokens; on a `cypher.runBFS` panic that surfaced a Homebrew Ruby `Pincher` class and `runGoInstall`/`runBrew` as false-positive suspects scoring nearly as high as the real culprit. Slash-delimited path segments are now dropped structurally and the panic-message vocabulary is in the stopword set. **`plan_change`** — on a hub symbol (hundreds of callers) the blast-radius envelope hit 61 KB and exceeded the MCP per-call token cap, returning nothing usable on exactly the symbols you most need to plan around; the per-list cap dropped from 50 to 25 and `test_files_intersecting` (previously uncapped) is now bounded too, with `truncated` set and the true counts preserved in `summary`. **`onboard_module`** — a module consumed only via cross-package method calls (which the CALLS graph under-resolves) showed an empty `external_consumers` and an all-test `external_dependencies` with no caveat, reading as "nothing depends on this module"; it now stamps a `boundary_calls_under_resolved` `_meta` warning when the boundary looks isolated. Found in the v0.89 composite-audit deep dive.
