Per-stage resolve-pass observability: each of the four resolvers (`IMPORTS` / `CALLS` / `READS` / `USES_VAR`) now emits a single `pincher.resolve.summary` `slog.Info` line carrying `kind` / `pending_in` / `resolved_out` / `dropped` / `duration_ms`. Quiet on no-op ticks (pending_in==0) so healthy projects don't spam logs; lit up on real resolve work so the next perf decision has data to argue from. Unlocks #1611 (parallelize resolve passes) — measurement-before-rewrite. (#1613)
