Extraction-phase wall-clock timing — every `Index()` run now emits `pincher.index.extraction.summary` carrying `files_extracted` / `files_hash_skipped` / `files_blocked` / `duration_ms` covering walker + per-file goroutine fan-out + final flush. Closes the last unmeasured indexing phase: extraction can now be compared directly against `pincher.resolve_block.summary` (#1613 v0.85) and `pincher.index.gc.summary` (this PR's sibling) — answering "where does the time go on this corpus?" without re-running with a profiler attached. Even no-change watcher ticks pay the walker cost; surfacing it makes a 2s tick on a 50k-file repo distinguishable from the expected 200ms steady state. (#1613 follow-up)
