`search` empty-results path now stamps `EmptyReasonLowConfidenceExtractor` when `min_confidence` filters out every FTS5 candidate (#1603 v0.84 follow-up). Pre-fix it stamped `EmptyReasonQueryTooNarrow` for both the filter-fix case and the threshold-too-tight case — `why_empty` answered both with the same generic "drop a filter" recovery. Now the threshold case gets the dedicated `low_confidence_extractor` code so `why_empty` can return the right recovery action ("drop the threshold"). Closes one of the four orphan-stamp gaps tracked in #1603 (down to 3/10 stamped→orphan).
