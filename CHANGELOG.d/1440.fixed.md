`context_for_task` task-driven path now uses the same AND→OR fallback that `search` does (server.go:5469). Prose tasks like "classify a corpus file as code or config" previously returned zero seeds — FTS5's default AND semantics required every prose token to appear in a single symbol's text — defeating the whole point of the composite tool. Now multi-token tasks retry as OR-joined queries when AND returns empty, matching `search`'s behaviour (#1440).
