Branch-drift advisory now skips projects whose on-disk path no longer exists (#1667 v0.87). Same-release dogfood-found polish: after #1665 silenced the testdata-corpus drift advisory, the surviving "real drift" entry on the dogfood machine was `probe` — a project whose source directory had been deleted but whose DB row persisted. The advisory's remediation (`pincher index <path>`) errors out on a non-existent path, training the user to ignore it. New `os.Stat` precheck skips dead-path projects so the advisory only fires when remediation actually applies; dead-path ghosts route through `list prune_dead=true` (the existing #378 cleanup path), which is the semantically correct fix. Mirrored to MCP doctor + CLI doctor per the bounded-duplication convention.
